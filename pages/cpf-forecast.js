import React from 'react'
import CPFForecastPage from '../components/views/cpfForecast'

// Not sure why, but the import from cpfCalculator page is not read when it's under another name
const cpfForecast = () => <CPFForecastPage />

export default cpfForecast
