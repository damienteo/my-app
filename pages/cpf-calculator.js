import React from 'react'
import AboutPage from '../components/views/cpfCalculator'

// Not sure why, but the import from cpfCalculator page is not read when it's under another name
const cpfCalculator = () => <AboutPage />

export default cpfCalculator
